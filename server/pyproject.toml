[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tornado-utils"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["tornadoutils*"]
